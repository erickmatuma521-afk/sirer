# Optionnel : URL de l’API si elle n’est pas sur le même domaine que le frontend.
# En production sur Vercel (frontend + API sur le même projet), laisser vide : les appels utilisent /api.
# VITE_API_BASE=https://api.example.com/api
